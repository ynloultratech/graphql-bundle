query GetNode($id: ID!){
    node(id: $id) {
        id
        ... on Post {
            title
            body
        }
    }
}

query GetNodes($ids: [ID!]!){
    nodes(ids: $ids) {
        id
        ... on Post {
            title
            body
        }
    }
}