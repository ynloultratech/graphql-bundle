imports:
    - { resource: config_dev.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

graphql:
    error_handling:
        show_trace: false
    security:
        validation_rules:
            query_complexity: 303

